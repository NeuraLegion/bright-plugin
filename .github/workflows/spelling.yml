name: Check Spelling

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  spelling:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check spelling
        uses: crate-ci/typos@master
        with:
          files: .
          isolated: true
